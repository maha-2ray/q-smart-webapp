import { PageLayout } from "../../components/layouts/page-layout";
import React from "react";

const Settings: React.FC = () => {
  return (
    <PageLayout title="Settings" subtitle="Manage your application settings">
      <div>Settings</div>
    </PageLayout>
  );
};

export default Settings;
